# main.js
// Aquí puedes colocar funciones JS reutilizables para la app web.

// Ejemplo: función para formatear monedaunction formatCurrency(value) {
    return "$" + Number(value).toLocaleString("es-CO", {minimumFractionDigits: 2});
}
